{
	"version": 1,
	"description": "Cloudflare Pages routing — functions for /api/*, SPA fallback for all other paths",
	"include": ["/*"],
	"exclude": ["/*.js", "/*.css", "/assets/*", "/icons.svg", "/favicon.svg"],
	"fallback": { "file": "/index.html" }
}
