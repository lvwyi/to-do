server {
    listen       80;
    server_name  localhost;

    root   /usr/share/nginx/html;
    index  index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;

    # AI 代理：转发到 Cloudflare Workers（藏 Key）
    location /api/ai {
        proxy_pass https://todo-ai-proxy.717377880.workers.dev;
        proxy_set_header Host todo-ai-proxy.717377880.workers.dev;
        proxy_set_header X-Forwarded-For $remote_addr;
    }

    # SPA 路由支持：所有不存在的文件都回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存（带 contenthash 的文件可长期缓存）
    location ~* \.(?:css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # favicon 等图标
    location ~* \.(png|svg|ico)$ {
        expires 30d;
    }

    # 安全头
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options SAMEORIGIN;
}
