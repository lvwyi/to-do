name = "todo-ai-proxy"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# ⚠️ API Key 不要写在这里！
# 开发：复制到 .dev.vars （已 .gitignore）
# 生产：部署时传参  wrangler deploy --var DASHSCOPE_API_KEY:sk-xxxxx
#        或在 Cloudflare 控制台 → Worker → Variables and Secrets
