name = "todo-ai-proxy"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# ⚠️ API Key 不要写在这里！
# 开发：复制到 .dev.vars （已 .gitignore）
# 生产：在 Cloudflare 控制台 → Worker → Variables and Secrets
#       设置 DIFY_API_KEY 和 DIFY_BASE_URL
